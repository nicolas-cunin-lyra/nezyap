apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: {{ include "nezyap.fullname" . }}-catalog-frontend
  namespace: {{ include "nezyap.name" . }}
  labels:
    {{- include "nezyap.labels" . | nindent 4 }}
    app.kubernetes.io/element: catalog-frontend
spec:
  triggers:
  - type: GitHub
    github:
      secretReference:
        name: {{ include "nezyap.fullname" . }}-nicolas-cunin-lyra-github-secret
  source:
      type: "Git"
      git:
        uri: https://github.com/nicolas-cunin-lyra/nezyap.git
        ref: feature/s2i
      contextDir: catalog-frontend
  strategy:
    type: Source
    sourceStrategy:
        from:
          kind: ImageStreamTag
          namespace: openshift
          name: nodejs:14-ubi8
        incremental: true
  output:
    to:
      kind: ImageStreamTag
      name: {{ include "nezyap.fullname" . }}-catalog-frontend:{{ .Values.image.version }}
      namespace: {{ include "nezyap.name" . }}
